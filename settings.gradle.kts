rootProject.name = "UnlimitedEnchant"

pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
    }

    plugins {
        id("io.papermc.paperweight.userdev") version("1.7.1")
    }
}
